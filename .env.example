# Demo CRM — Book a Demo form endpoint
# Leave empty for preview mode (simulated success, logs to console).
# For HubSpot/Zoho/Custom webhook: set to POST endpoint that accepts JSON.
VITE_CRM_ENDPOINT=https://your-crm.example.com/api/demo-request
# Optional: used for analytics labeling
VITE_CRM_PROVIDER=custom
